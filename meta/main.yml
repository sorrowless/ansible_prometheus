galaxy_info:
  author: Stanislaw Bogatkin
  description: Ansible role to install Prometheus server and clients
  company: Personal project

  license: Apache

  min_ansible_version: 2.7

  platforms:
  - name: Ubuntu
    versions:
    - xenial

  galaxy_tags: [prometheus]

  dependencies:
    - src: jdauphant.nginx
      version: v2.21.2
      name: nginx
  # List your role dependencies here, one per line. Be sure to remove the '[]' above,
  # if you add dependencies to this list.
